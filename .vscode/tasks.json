{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cat",
            "type": "shell",
            "command": "tail -1 main.go | go run main.go --conf diff",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}